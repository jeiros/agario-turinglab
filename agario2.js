//link to  BG image
var gridBG = 'pics/agarbg.png';

//load background image before game starts
function preload(){
  bg = loadImage(gridBG);
}



//Setup runs once at the beginning
function setup() {
  createCanvas(500,500);
  background(loadImage(gridBG));
}

//Draw runs forever 
function draw() {
 
  
}
